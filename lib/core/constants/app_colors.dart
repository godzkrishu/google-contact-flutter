import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors{
  AppColors._();
  static const Color primary = Color(0xFF1E1E2E); // deep premium dark
  static const Color accent = Color(0xFF6C63FF);  // soft premium purple
  static const Color background = Color(0xFFF7F7FB);
  static const Color card = Colors.white;
}